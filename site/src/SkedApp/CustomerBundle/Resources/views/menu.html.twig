<div class="row-fluid">
    <ul class="nav nav-tabs pull-right">
        {% if is_granted('ROLE_SITE_USER') %}
            {% if controller == 'bookings'%}
                <li class="active"><a href="{{ path('sked_app_customer_list_bookings' , {'id':customer.id }) }}" title="my bookings">Bookings</a></li>
            {% else %}
                <li><a href="{{ path('sked_app_customer_list_bookings' , {'id':customer.id }) }}" title="my bookings">Bookings</a></li>
            {% endif %}
        {% endif%}
    </ul>
</div> <!-- End .row-fluid -->
<div class="row-fluid nav-active"><!-- This is just a spacer to show which page is active --></div>
<div class="row-fluid content-devider"><!-- This is just a visual devider to seperate the content from the navigation --></div>
